import { ChangeElDirective } from './change-el.directive';

describe('ChangeElDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeElDirective();
    expect(directive).toBeTruthy();
  });
});
